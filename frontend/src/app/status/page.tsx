'use client'
import '../globals.css'
import SeeArticles from '@/components/ArticleStatus';

export default function status() {
    return (
        <main>
            <SeeArticles />
        </main>
    );
}